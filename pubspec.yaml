name: rancho_paracatu_mobile
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  # Pacotes adicionados para a Etapa A
  provider: ^6.1.2
  http: ^1.2.1
  supabase_flutter: ^2.14.2
  image_picker: ^1.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    # Barra invertida corrigida abaixo
    - assets/images/rustic-bg.png